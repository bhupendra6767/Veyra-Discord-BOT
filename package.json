{
  "name": "veyra-bot",
  "version": "1.0.0",
  "scripts": {
    "start": "python3 -m venv venv && . venv/bin/activate && pip install -r requirements.txt && python3 main.py",
    "build": "echo 'No build step'"
  }
}
